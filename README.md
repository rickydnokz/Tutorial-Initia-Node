# Tutorial Initia Node

### 1. UPDATE SYSTEM AND INSTALL BUILD TOOLS
```
sudo apt -q update
sudo apt install curl tar wget clang pkg-config libssl-dev jq build-essential bsdmainutils git make ncdu gcc git jq chrony liblz4-tool -y
sudo apt -qy upgrade
```

### 2. 
